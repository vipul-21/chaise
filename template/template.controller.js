(function () {
    'use strict';

    angular.module('chaise.template')

    .controller('templateController',['$document', '$rootScope', '$scope', '$timeout', 'UiUtils', '$window',
        function ($document, $rootScope, $scope, $timeout, UiUtils, $window) {
            
        }
    ]);
})();
